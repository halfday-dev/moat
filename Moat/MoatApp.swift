import SwiftUI

/// Minimal macOS host app for the Moat content filter system extension.
@main
struct MoatApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
